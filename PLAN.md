# خطة تنفيذ نظام دعم العملاء ومراقبة المواقع (MKSS)

> وثيقة التنفيذ المرجعية. كل قرار هنا متشرح بسببه وبدايله.
> آخر تحديث: 2026-09-13

---

## 1) الهدف في جملة واحدة

بوابة يدخلها العميل بإيميل وباسورد، فيشوف **حالة موقعه بشكل احترافي**: شغّال ولا واقع، منصته إيه
(ووردبريس / شوبيفاي / سلة / غيره)، شهادة الأمان بتاعته، سرعته، آخر صيانة اتعملت له،
والمتأخرات المالية عليه — وفوق الصفحة **زرار المنتج (MVP) بتاعه**.

---

## 2) القرار المعماري: صفر اعتماد خارجي

بناءً على طلبك الصريح: **مفيش أي حاجة مبنية على حاجة خارجية**.

| الوظيفة | الحل الداخلي المستخدم | البديل الخارجي المرفوض | ليه رفضناه |
|---|---|---|---|
| قاعدة البيانات | `node:sqlite` (مدمج في Node 22) | Supabase / PlanetScale | اشتراك + انقطاع الخدمة يوقّف نظامك |
| سيرفر الويب | `node:http` (مدمج) | Express / Next.js | مكتبات + ثغرات تبعيات |
| كشف نوع المنصة | محرك بصمات كتبناه بأنفسنا | Wappalyzer / BuiltWith API | سعر لكل طلب + حد أقصى |
| فحص SSL | `node:tls` (مدمج) | SSL Labs API | بطيء + محدود بعدد فحوصات |
| قياس السرعة | قياس TTFB بنفسنا | Google PageSpeed API | مفتاح API + حصة يومية |
| مراقبة التوقف | جدولة داخلية | UptimeRobot / Pingdom | اشتراك شهري لكل موقع |
| تشفير كلمات السر | `scrypt` من `node:crypto` | Auth0 / Firebase Auth | بياناتك عند طرف تالت |
| الواجهة | HTML/CSS/JS خام | React + خطوط CDN | تبعيات + طلبات لسيرفرات برّه |

**النتيجة العملية:** `npm install` **لا يُنفَّذ إطلاقًا**. ملف `package.json` فيه
`"dependencies": {}` فاضية. النظام بيشتغل بأمر واحد: `node server.js`.
يشتغل على أي سيرفر فيه Node 22، وعلى لابتوب بدون إنترنت، وبياناتك كلها في ملف واحد
اسمه `data/mkss.db` تقدر تنسخه وتنقله.

---

## 3) الحقيقة اللي لازم تتبني عليها: طبقات الربط

دي أهم نقطة في الوثيقة كلها. جملة "يتربط بأي موقع" ليها تفصيل:

### Tier 0 — الفحص الخارجي (✅ شغّال على أي موقع في الدنيا، بدون إذن من حد)

النظام بيتصرف زي أي زائر: بيفتح الموقع ويقيس ويقرأ. مش محتاج باسورد ولا بلَجن ولا تعاون من العميل.

يقدر يعرف: الموقع شغّال ولا واقع · كود الاستجابة · زمن أول بايت (TTFB) · الزمن الكلي ·
حجم الصفحة · سلسلة التحويلات · شهادة SSL كاملة (صلاحيتها، مُصدِرها، أيام متبقية، بتغطي الدومين ولا لأ) ·
الـ DNS وسجلاته ومزوّده · هيدرات الأمان · **نوع المنصة ونسختها** · التقنيات والإضافات المستخدمة ·
البيكسلات (Meta / TikTok / GA4) · هل الموقع متجاوب ولا لأ.

**ده وحده بيعمل داشبورد محترم جدًا للعميل. وده هو v1.**

### Tier 1 — داخل ووردبريس (🔑 محتاج إذن لمرة واحدة)

لقراءة الإضافات المحتاجة تحديث، نسخة PHP، الباكب، سجل الأخطاء — لازم الموقع يدّي إذن.
طريقتان، والاتنين آمنين:
- **Application Password** من ووردبريس (باسورد منفصل، صلاحيات محدودة، بيتلغى بضغطة).
- **بلَجن ربط صغير** نكتبه إحنا، بيتركّب على موقع العميل ويطلّع مفتاح.

> ⛔ **قاعدة حمراء:** ممنوع نهائيًا نطلب من العميل باسورد الأدمن بتاع موقعه.
> ده خطر أمني وقانوني عليك أنت. ولو حصل تسريب، المسؤولية مسؤوليتك.

### Tier 2 — شوبيفاي وغيره (🔑 محتاج توكن من العميل)

شوبيفاي بيحتاج Custom App لكل متجر (العميل بيعمله ويدّيك التوكن) أو Public App
(محتاج حساب Partner وموافقة من شوبيفاي — **اعتماد خارجي، مؤجَّل**).
ويكس وسكوير سبيس: مفيش API مفيد — Tier 0 بس.

### Tier 3 — بياناتك أنت (✅ مفيش أي قيود)

الصيانة، الفواتير، المتأخرات، التذاكر. دي بياناتك في قاعدة بياناتك، بتدخلها من لوحة الأدمن.

---

## 4) شجرة الملفات ودور كل ملف

```
mkss/
├── server.js                  نقطة الدخول: يشغّل HTTP + المراقب
├── seed.js                    بيانات تجريبية للتشغيل الأول
├── package.json               صفر تبعيات
│
├── src/
│   ├── schema.sql             12 جدول SQL
│   ├── db.js                  اتصال SQLite + اختصارات الاستعلام
│   ├── auth.js                scrypt + جلسات + CSRF + تحديد المحاولات
│   ├── http-util.js           راوتر + كوكيز + قراءة الـ body + تهريب HTML
│   │
│   ├── checks/                ← محرك الفحص (قلب النظام)
│   │   ├── fetch.js           جالب HTTP خام بقياس زمني دقيق
│   │   ├── tls.js             فحص شهادة SSL عبر TLS مباشر
│   │   ├── dns.js             فحص DNS والسجلات والمزوّد
│   │   ├── fingerprint.js     محرك بصمات المنصات (26 منصة)
│   │   ├── headers.js         تقييم هيدرات الأمان
│   │   └── run.js             المنسّق: يجمع الفحوصات ويحسب الدرجة
│   │
│   ├── monitor.js             الجدولة + فتح وإغلاق الأعطال
│   ├── routes/                مسارات الويب (auth / client / admin / api)
│   └── views/                 قوالب HTML بالعربي RTL
│
├── public/                    CSS + JS للمتصفح (بدون CDN)
├── test/                      اختبارات بـ node:test المدمج
└── data/mkss.db               قاعدة بياناتك (خارج Git)
```

---

## 5) نموذج البيانات — 12 جدول

| الجدول | بيخزن إيه | العلاقة |
|---|---|---|
| `users` | العملاء والأدمنز. فيه `mvp_url` و `mvp_label` لزرار المنتج | الأصل |
| `sites` | مواقع العميل + المنصة المكتشفة + فترة الفحص | → users |
| `checks` | كل نتيجة فحص (سجل تاريخي كامل) | → sites |
| `incidents` | الأعطال: بدأت امتى وخلصت امتى | → sites |
| `maintenance` | سجل الصيانة: نوعها، تفاصيلها، مين عملها، الموعد الجاي | → sites |
| `invoices` | الفواتير ومبالغها وحالتها | → users, sites |
| `payments` | الدفعات على كل فاتورة (يدعم الدفع الجزئي) | → invoices |
| `tickets` | تذاكر الدعم | → users, sites |
| `ticket_messages` | رسائل كل تذكرة | → tickets |
| `sessions` | جلسات الدخول | → users |
| `settings` | إعدادات النظام (منها سر التوقيع) | — |

**ليه `checks` جدول منفصل ومش عمود في `sites`؟**
عشان نقدر نحسب نسبة التشغيل (uptime %) على 30 يوم، ونرسم منحنى السرعة،
ونقول للعميل "موقعك كان أسرع الشهر اللي فات" — ده اللي بيخلي التقرير احترافي بدل لقطة لحظية.

---

## 6) إزاي كل فحص بيشتغل (بالتفصيل)

### أ) فحص التشغيل والسرعة — `fetch.js`
بنفتح اتصال HTTP خام ونقيس بـ `process.hrtime.bigint()` (دقة نانوثانية):
- **TTFB**: من بداية الطلب لأول بايت راجع → بيقيس سرعة السيرفر نفسه.
- **الزمن الكلي**: لحد آخر بايت → بيقيس السيرفر + حجم الصفحة.
- بنقرأ **512KB بس** من الصفحة — كفاية للبصمة ومبتستهلكش ذاكرة.
- بنتتبع التحويلات يدويًا (5 كحد أقصى) عشان نكشف حلقات التحويل والتحويلات الزيادة.
- بنحوّل أكواد أخطاء Node لرسائل عربية (`ENOTFOUND` → "الدومين غير موجود أو الـ DNS مش شغال").

### ب) فحص SSL — `tls.js`
اتصال TLS مباشر مع **SNI** (ضروري للاستضافة المشتركة). بنقرأ الشهادة زي ما المتصفح بيقرأها:
صالحة؟ · منتهية؟ · كام يوم فاضل؟ · مين مُصدِرها؟ · **بتغطي الدومين ده فعلًا؟** (مع دعم الـ wildcard صح:
`*.example.com` بتغطي `shop.example.com` بس مش `a.b.example.com`).

### ج) فحص DNS — `dns.js`
بنحل الدومين ونقرأ A / AAAA / NS / MX / TXT، ونستنتج المزوّد من الـ nameservers
(Cloudflare, AWS, Hostinger, GoDaddy...) بدون WHOIS خارجي. وبنشوف SPF و DMARC للإيميل.

### د) بصمة المنصة — `fingerprint.js`
**ده الجزء اللي بيحل محل Wappalyzer.** منظومة أوزان:

كل منصة ليها قواعد، وكل قاعدة ليها وزن حسب قطعيتها:
```
ووردبريس:  وسم generator = 50    wp-content = 45    wp-includes = 40
           كوكي wordpress_ = 35   wp-json = 30
شوبيفاي:   هيدر x-shopify-stage = 55   cdn.shopify.com = 50   كائن Shopify = 50
```
بنجمع أوزان كل منصة، والأعلى بتفوز، والمجموع بيتحول لنسبة ثقة.
- بنميّز **الإضافات** (ووكومرس) عن **المنصات** فما يتنافسوش.
- بنكشف كمان: Elementor · Divi · GSAP · Tailwind · React · GA4 · Meta Pixel · TikTok Pixel · واتساب.
- **يشمل السوق العربي:** سلة و زد — مش موجودين في أغلب الأدوات الأجنبية.
- 26 منصة مدعومة حاليًا.

### هـ) هيدرات الأمان — `headers.js`
HSTS · CSP · X-Frame-Options · X-Content-Type-Options · Referrer-Policy · Permissions-Policy
+ كشف تسريب نسخة السيرفر (`X-Powered-By: PHP/7.4` بيقول للمخترق نسختك).

---

## 7) معادلة درجة الصحة (0–100)

مش رقم عشوائي — ده الوزن الفعلي:

| البند | الوزن | إزاي بيتحسب |
|---|---:|---|
| التشغيل | 40 | واقع = 0 · كود 5xx = 10 · 4xx = 25 · 2xx = 40 |
| SSL | 20 | صالحة وأكتر من 21 يوم = 20 · أقل = 12 · أقل من 7 = 5 · باطلة = 0 |
| السرعة | 15 | أقل من 400ms = 15 · أقل من 800 = 12 · أقل من 1500 = 9 · أقل من 3000 = 5 · غير كده = 2 |
| هيدرات الأمان | 15 | نسبة الهيدرات الموجودة من المطلوبة |
| الدومين والـ DNS | 10 | يحل صح = 10 · جزئي = 5 · فاشل = 0 |

**التقدير:** 90+ ممتاز · 75+ جيد · 60+ يحتاج انتباه · 40+ مشاكل · أقل = حرج.

كل نقصان بيطلع **بسبب مكتوب بالعربي وحل مقترح** — مش مجرد رقم أحمر. ده اللي بيخلي
العميل يحس إن فيه حد شغال على موقعه فعلًا.

---

## 8) تدفق الاستخدام

### العميل (بيدخل بإيميل + باسورد)
```
تسجيل دخول
   ↓
[ زرار المنتج/MVP بتاعك ] ← فوق خالص، بحالته لايف
   ↓
ملخص: عندك 3 مواقع · 2 ممتاز · 1 يحتاج انتباه · عليك 4,500 ج.م متأخرة
   ↓
كروت المواقع: الاسم · الحالة · شارة المنصة · الدرجة · آخر صيانة
   ↓
صفحة الموقع: منحنى السرعة · سجل الأعطال · سجل الصيانة · الفواتير · فتح تذكرة
```

### الأدمن (أنت)
```
لوحة الأدمن
   ↓
إضافة عميل (اسم، إيميل، باسورد مبدئي، رابط الـ MVP بتاعه)
   ↓
إضافة موقع → النظام يكتشف منصته تلقائيًا في أول فحص
   ↓
تسجيل صيانة · إصدار فاتورة · تسجيل دفعة · الرد على التذاكر
   ↓
زرار "افحص الآن" لأي موقع
```

---

## 9) زرار الـ MVP

بيتخزن في `users.mvp_url` و `users.mvp_label` — **لكل عميل زرار مختلف**.
بيظهر فوق الداشبورد كبطاقة بارزة فيها:
اسم المنتج · حالته اللحظية (شغّال / واقع) · درجة صحته · زرار "افتح المنتج".

لو العميل معندوش MVP متسجل، البطاقة بتختفي تلقائيًا.

---

## 10) القواعد الأمنية (غير قابلة للتفاوض)

1. كلمات السر بـ **scrypt** (N=16384) مع ملح عشوائي لكل مستخدم — مش MD5 ولا SHA بسيط.
2. المقارنة بـ `timingSafeEqual` — لمنع هجمات التوقيت.
3. الكوكيز `HttpOnly` + `SameSite=Lax` (+ `Secure` مع HTTPS).
4. **CSRF** على كل طلب POST — توكن مشتق من الجلسة بـ HMAC.
5. **تحديد محاولات الدخول**: 8 محاولات كل 15 دقيقة لكل إيميل+IP.
6. **عزل البيانات**: كل استعلام بيتفلتر بـ `user_id` — العميل مستحيل يشوف بيانات عميل تاني.
   (ده أخطر بند في النظام كله — تسريب واحد بيقتل السمعة.)
7. كل الاستعلامات **parameterized** — مفيش تركيب SQL نصيًا أبدًا.
8. تهريب HTML على كل مُخرَج — لمنع XSS.
9. **ممنوع تخزين باسورد أدمن أي موقع عميل.**

---

## 11) مراحل التنفيذ

### ✅ المرحلة 1 — الأساس (خلصت)
- [x] هيكل المشروع وصفر تبعيات
- [x] 12 جدول SQL
- [x] طبقة قاعدة البيانات (`db.js`)
- [x] المصادقة الكاملة (`auth.js`)
- [x] جالب HTTP بقياس زمني — **مُختبَر حيًا**
- [x] فحص SSL — **مُختبَر حيًا**
- [x] فحص DNS — **مُختبَر حيًا**
- [x] محرك البصمات (26 منصة)

### 🔄 المرحلة 2 — إغلاق محرك الفحص
- [ ] `headers.js` — تقييم هيدرات الأمان
- [ ] `run.js` — منسّق الفحص + معادلة الدرجة
- [ ] `test/` — اختبارات محلية للبصمات (ضرورية: البيئة الحالية بتحجب المواقع الخارجية)
- [ ] `monitor.js` — الجدولة وفتح/إغلاق الأعطال

### 🔄 المرحلة 3 — الويب
- [ ] راوتر + جلسات + CSRF في الطلبات
- [ ] صفحة الدخول
- [ ] داشبورد العميل + زرار MVP
- [ ] صفحة تفاصيل الموقع + المنحنيات
- [ ] الفواتير والمتأخرات
- [ ] التذاكر
- [ ] لوحة الأدمن

### 🔄 المرحلة 4 — التسليم
- [ ] بيانات تجريبية (`seed.js`)
- [ ] دليل التشغيل والنشر
- [ ] فحص عدائي قبل التسليم

### ⏭️ بعد v1 (لما توافق)
- Tier 1: بلَجن ربط ووردبريس
- تنبيهات إيميل عند التوقف (SMTP مباشر بـ `node:net` — برضه بدون خدمة خارجية)
- Tier 2: شوبيفاي
- تقارير PDF شهرية

---

## 12) القيود الحقيقية (بصراحة كاملة)

1. **البيئة الحالية بتحجب الإنترنت الخارجي** (`Host not in allowlist`)، وبتستبدل شهادات SSL
   ببروكسي. يعني الفحوصات هنا مش هتدي أرقام حقيقية. الكود سليم، بس **التحقق النهائي لازم
   يتعمل على سيرفرك أنت**. عشان كده هنبني اختبارات محلية بملفات HTML مصغّرة.
2. **كشف المنصة بالفحص الخارجي دقته ~85–90%** للمنصات الكبيرة. المواقع اللي ورا Cloudflare
   بوضع صارم أو صفحات مبنية بالكامل بـ JS ممكن تديك ثقة أقل. الحل: النظام بيعرض **نسبة الثقة
   والدليل**، وتقدر تكتب المنصة يدويًا لو عرفتها.
3. **المراقبة من مكان واحد**: لو سيرفرك وقع، المراقبة بتقف. الحل الصح لاحقًا: نسختين في
   مكانين مختلفين. (دي مشكلة كل نظام مراقبة، وUptimeRobot بيحلها بمراكز متعددة.)
4. **SQLite ممتازة لحد ~5000 موقع** على سيرفر واحد. فوق كده تحتاج Postgres.
   طبقة `db.js` معزولة عشان النقل يبقى سهل.
