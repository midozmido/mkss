# خطة تنفيذ نظام دعم العملاء ومراقبة المواقع (MKSS)

> النسخة 2 — معادة الكتابة بعد تثبيت القيدين المقدسين.
> آخر تحديث: 2026-09-13 · الفرع: `claude/customer-support-sites-system-qm032c`

---

## 1. الهدف والمبادئ الحاكمة

بوابة يدخلها العميل **بإيميل وباسورد فقط**، فيرى حالة موقعه باحترافية: شغّال أم واقع،
منصته (ووردبريس / شوبيفاي / سلة / زد / غيرها)، شهادة الأمان، السرعة، آخر صيانة،
والمتأخرات المالية — وفوق الصفحة زرار المنتج (MVP) الخاص به.

### القيد المقدّس الأول — صفر اعتماد خارجي
> "مش عايز اى حاجه مبنيه على حاجه خارجية ابدا"

**ممنوع:** أي حزمة npm · أي خدمة SaaS · أي CDN أو خط خارجي.
**مسموح فقط:** وحدات Node 22 المدمجة + HTML/CSS/JS خام.
**الاختبار العملي:** `package.json` فيه `"dependencies": {}` و `npm install` لا يُنفَّذ إطلاقًا.

### القيد المقدّس الثاني — العميل يدخل ويشوف، ولا يفعل شيئًا
> "اهم حاجه عندى ان العميل يخش بايميل وباسورد بس ميدخلش ف حوارات تانية خالص"

العميل **عمره ما** يركّب بلَجن، ولا ينسخ توكن، ولا يعمل Application Password، ولا يضيف
موقعًا، ولا يضبط إعدادًا، ولا يجيب عن سؤال. كل الإعداد من لوحة الأدمن.

---

## 2. ما تغيّر عن النسخة الأولى ولماذا

| البند | النسخة 1 | النسخة 2 | السبب |
|---|---|---|---|
| طبقات الربط | 4 طبقات (Tier 0→3) | **طبقة واحدة: الفحص الخارجي** | Tier 1 و 2 يتطلبان عملًا من العميل — مخالف للقيد الثاني |
| بلَجن ووردبريس | في خارطة الطريق | **محذوف نهائيًا** | يتطلب تركيبًا من العميل |
| توكن شوبيفاي | مؤجَّل | **محذوف نهائيًا** | يتطلب نسخًا ولصقًا من العميل |
| إضافة المواقع | غير محدد | **الأدمن فقط** | العميل لا يدخل بيانات |
| استعادة كلمة السر | غير محلولة | **طلب يظهر للأدمن + رابط لمرة واحدة** | لا خدمة إيميل، ولا مجهود على العميل |
| فلسفة الواجهة | عرض أرقام | **سرد حالة الموقع بجُمل** | القيمة كلها فيما يراه، لأنه لا يضبط شيئًا |

**النتيجة:** الخطة صارت أبسط وأقوى. حُذف كل ما يحتاج تعاونًا من العميل، فاختفت معه
كل مخاطر التوكنات المخزَّنة والمسؤولية القانونية عن بيانات دخول مواقع الغير.

---

## 3. المعمار العام

```
┌─────────────────────────────────────────────────────────┐
│  المتصفح — HTML/CSS/JS خام · RTL · بدون CDN ولا build    │
└────────────────────────┬────────────────────────────────┘
                         │ HTTP
┌────────────────────────▼────────────────────────────────┐
│  server.js — node:http · راوتر · جلسات · CSRF            │
├─────────────────────────────────────────────────────────┤
│  routes/  →  auth · client · admin                       │
├─────────────────────────────────────────────────────────┤
│  monitor.js — جدولة · أعطال · سجل فجوات                  │
├─────────────────────────────────────────────────────────┤
│  checks/  →  guard · fetch · tls · dns · fingerprint      │
│              headers · run (منسّق + درجة الصحة)          │
├─────────────────────────────────────────────────────────┤
│  db.js — node:sqlite (WAL) · auth.js — scrypt            │
└────────────────────────┬────────────────────────────────┘
                         │
                  data/mkss.db  (ملف واحد — انسخه وانقله)
```

| الوظيفة | الحل الداخلي | البديل الخارجي المرفوض |
|---|---|---|
| قاعدة البيانات | `node:sqlite` | Supabase / PostgreSQL مستضاف |
| سيرفر الويب | `node:http` | Express / Next.js |
| كشف المنصة | محرك بصمات داخلي (26 منصة) | Wappalyzer / BuiltWith |
| فحص SSL | `node:tls` | SSL Labs API |
| قياس السرعة | TTFB بـ `process.hrtime.bigint()` | Google PageSpeed API |
| مراقبة التوقف | جدولة داخلية | UptimeRobot / Pingdom |
| كلمات السر | `scrypt` من `node:crypto` | Auth0 / Firebase |
| الخطوط | خطوط النظام | Google Fonts |
| المخططات | SVG مولّد من السيرفر | Chart.js / D3 |

---

## 4. نموذج الوصول والعزل

أخطر بند في النظام: **تسريب بيانات عميل لعميل آخر**. الحل بالبناء لا بالانضباط:

1. كل دوال الاستعلام في `src/repo.js` تأخذ `userId` **كوسيط أول إجباري**.
2. **ممنوع** أي استعلام خام (`db.prepare`) داخل ملفات `routes/`.
3. كل مسار فيه معرّف (`/site/12`) يمر على `assertOwnership(userId, kind, id)` قبل أي قراءة.
4. الأدمن له دوال منفصلة بأسماء تبدأ بـ `admin` — لا لبس بين المسارين.
5. اختبار آلي يثبت أن عميلًا لا يستطيع قراءة صف عميل آخر (في `test/isolation.test.js`).

---

## 5. المصادقة ودورة حياة الحساب

### إنشاء الحساب
الأدمن ينشئ العميل (اسم · إيميل · رابط MVP). النظام يولّد **رابط تفعيل موقّعًا لمرة واحدة**
صالحًا 72 ساعة. الأدمن يرسله للعميل بالطريقة التي يفضلها (واتساب غالبًا).
العميل يفتح الرابط ← يضع كلمة سره ← دخل. **خطوة واحدة، مرة واحدة في العمر.**

### استعادة كلمة السر بدون خدمة إيميل
لا توجد استعادة ذاتية. في صفحة الدخول زر **«طلب إعادة تعيين»**:
1. العميل يضغطه ← يُسجَّل طلب في جدول `reset_requests`.
2. يظهر الطلب في لوحة الأدمن مع تنبيه.
3. الأدمن يضغط «توليد رابط» ← رابط موقّع لمرة واحدة صالح 24 ساعة ← يرسله للعميل.

**لماذا هذا هو الحل الصحيح:** صفر خدمة خارجية · صفر مجهود على العميل ·
ومناعة كاملة ضد استغلال الخاصية لإقفال حسابات الآخرين، لأن لا شيء يحدث تلقائيًا.

### ضوابط إضافية
- الرسالة وزمن الاستجابة **متطابقان** سواء كان الإيميل موجودًا أم لا (منع تعداد الحسابات).
  ملاحظة حرجة: `scrypt` بطيء، والحساب غير الموجود يرجع فورًا ويفضح نفسه —
  لذا نُشغّل `scrypt` على تجزئة وهمية عند عدم وجود الحساب.
- تدوير معرّف الجلسة بعد الدخول (منع session fixation).
- تغيير كلمة السر يُبطل كل الجلسات الأخرى.
- تحديد المحاولات: 8 كل 15 دقيقة، بالإيميل **وبالـ IP** معًا.

---

## 6. نموذج البيانات

الجداول الحالية في `src/schema.sql` (12 جدولًا). التعديلات المطلوبة:

```sql
-- 1) الفلوس بالقروش (أعداد صحيحة) — REAL يسبب أخطاء تقريب
ALTER TABLE invoices ADD COLUMN amount_cents INTEGER NOT NULL DEFAULT 0;
ALTER TABLE payments ADD COLUMN amount_cents INTEGER NOT NULL DEFAULT 0;

-- 2) طلبات إعادة التعيين
CREATE TABLE reset_requests (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id      INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  requested_at TEXT NOT NULL,
  ip           TEXT,
  status       TEXT NOT NULL DEFAULT 'pending',  -- pending | issued | used | denied
  token_hash   TEXT,
  expires_at   TEXT
);

-- 3) رابط التفعيل / إعادة التعيين لمرة واحدة
CREATE TABLE one_time_links (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id    INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  kind       TEXT NOT NULL,          -- activate | reset
  token_hash TEXT NOT NULL UNIQUE,   -- نخزّن التجزئة لا التوكن
  created_at TEXT NOT NULL,
  expires_at TEXT NOT NULL,
  used_at    TEXT
);

-- 4) فجوات المراقبة — أمانة تجاه العميل
CREATE TABLE monitor_gaps (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  started_at TEXT NOT NULL,
  ended_at   TEXT NOT NULL,
  reason     TEXT           -- restart | shutdown | unknown
);

-- 5) تجميعات يومية — لمنع تضخّم جدول checks
CREATE TABLE checks_daily (
  site_id     INTEGER NOT NULL REFERENCES sites(id) ON DELETE CASCADE,
  day         TEXT NOT NULL,        -- YYYY-MM-DD
  checks      INTEGER NOT NULL,
  ok_checks   INTEGER NOT NULL,
  avg_ms      INTEGER,
  p95_ms      INTEGER,
  min_health  INTEGER,
  avg_health  INTEGER,
  PRIMARY KEY (site_id, day)
);

-- 6) سجل تدقيق
CREATE TABLE audit_log (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  at         TEXT NOT NULL,
  actor_id   INTEGER,
  action     TEXT NOT NULL,
  target     TEXT,
  detail     TEXT,
  ip         TEXT
);

-- 7) نسخة المخطط — لنظام هجرات بسيط
CREATE TABLE schema_version (version INTEGER PRIMARY KEY, applied_at TEXT NOT NULL);
```

**نمو البيانات:** 100 موقع × فحص كل 5 دقائق = 8.6 مليون صف سنويًا.
الحل: وظيفة ليلية تُجمِّع فحوصات اليوم السابق في `checks_daily` ثم تحذف
صفوف `checks` الأقدم من 30 يومًا. الاستعلامات الطويلة (uptime لـ30 يومًا) تقرأ من التجميع.

---

## 7. محرك الفحص

| الفحص | الملف | يقيس |
|---|---|---|
| حارس SSRF | `checks/guard.js` | يمنع الطلبات للشبكات الداخلية — **يعمل قبل أي جلب** |
| التشغيل والسرعة | `checks/fetch.js` | TTFB · الزمن الكلي · الحجم · التحويلات · كود الحالة |
| شهادة SSL | `checks/tls.js` | الصلاحية · الأيام المتبقية · المُصدِر · تغطية الدومين |
| الـ DNS | `checks/dns.js` | التحليل · السجلات · المزوّد · SPF/DMARC |
| بصمة المنصة | `checks/fingerprint.js` | المنصة · النسخة · الإضافات · البيكسلات (26 منصة) |
| هيدرات الأمان | `checks/headers.js` | HSTS · CSP · X-Frame-Options · تسريب نسخة السيرفر |
| المنسّق | `checks/run.js` | يشغّل الكل ويحسب درجة الصحة |

---

## 8. معادلة درجة الصحة (0–100)

| البند | الوزن | التفصيل |
|---|---:|---|
| التشغيل | 40 | 2xx = 40 · 3xx = 35 · 4xx = 25 · 5xx = 10 · واقع = 0 |
| SSL | 20 | صالحة >21 يوم = 20 · 8–21 يوم = 12 · ≤7 أيام = 5 · باطلة أو HTTP = 0 |
| السرعة | 15 | <400ms = 15 · <800 = 12 · <1500 = 9 · <3000 = 5 · غير ذلك = 2 |
| هيدرات الأمان | 15 | نسبة الموجود من المطلوب × 15 |
| الدومين والـ DNS | 10 | يحل بالكامل = 10 · جزئيًا = 5 · فشل = 0 |

**حالات حدّية محسومة:**
- الموقع واقع ← الدرجة الكلية = 0 مهما كان الباقي (لا معنى لـ"شهادة ممتازة" لموقع لا يفتح).
- موقع HTTP بلا تشفير ← سقف الدرجة 60 مهما كان الباقي.
- فشل الـ DNS ← الدرجة = 0 (الموقع غير موجود أصلًا).

**التقدير:** 90+ ممتاز · 75+ جيد · 60+ يحتاج انتباه · 40+ مشاكل · أقل = حرج.
كل نقصان يُنتج **سببًا بالعربي وحلًا مقترحًا** — لا رقمًا أحمر مجردًا.

---

## 9. محرك المراقبة

### الجدولة
حلقة كل 30 ثانية تختار المواقع المستحقة. **توزيع عشوائي (jitter)** حتى لا تتكدس
الفحوصات في نفس اللحظة. حد أقصى 5 فحوصات متزامنة. قفل لكل موقع يمنع تداخل الدورات.

### منع الإنذارات الكاذبة
1. الفشل الأول ← **لا إعلان**. إعادة محاولة بعد 60 ثانية.
2. فشلان متتاليان ← إعلان عطل.
3. **فحص مرجعي**: قبل إعلان أي عطل، نفحص هدفًا مرجعيًا. لو هو أيضًا فشل ←
   المشكلة في اتصالنا نحن ← **لا نعلن عطلًا على العميل** ونسجّل فجوة مراقبة.
4. منع الرفرفة: موقع يقع ويرجع خلال أقل من 5 دقائق يُسجَّل كعطل واحد لا عشرة.

### سجل الفجوات — بند أمانة
عند إقلاع النظام، لو آخر فحص مسجَّل أقدم من ضعف فترة الفحص، تُسجَّل **فجوة مراقبة**.
في تقرير العميل تظهر صراحة: «لم نكن نراقب من 03:10 إلى 05:40».
**لا نحسب الفجوة كوقت تشغيل.** نظام يكذب مرة واحدة يخسر الثقة للأبد.

### أمانة قياس السرعة
الرقم يُعرض دائمًا مع سياقه: «من سيرفر المراقبة في [الموقع الجغرافي]» —
لا يُقدَّم أبدًا كأنه تجربة كل الزوار.

---

## 10. الواجهة — "البرنامج 2 في 1"

الترتيب من أعلى لأسفل في لوحة العميل:

1. **بطاقة المنتج (MVP)** — بارزة، بحالتها اللحظية وزر «افتح المنتج».
   تختفي تلقائيًا لو العميل بلا `mvp_url`.
2. **شريط الملخص** — 3 مواقع · 2 ممتاز · 1 يحتاج انتباه · متأخرات 4,500 ج.م
3. **كروت المواقع** — الاسم · الحالة · شارة المنصة · الدرجة · آخر صيانة
4. **آخر النشاط** — آخر صيانة وآخر عطل

### الفلسفة: الواجهة تحكي ولا تعرض أرقامًا
> «موقعك شغّال من **47 يوم** بدون انقطاع · آخر صيانة من **12 يوم** ·
> شهادة الأمان تنتهي بعد **45 يوم** وسنجددها قبلها»

بدلًا من «Uptime 99.97% · SSL valid». العميل غير التقني يفهم الجُمل لا النِّسب.

### قواعد ملزمة
- **RTL** بالخصائص المنطقية (`margin-inline`, `padding-block`) لا `left/right`.
- **خطوط النظام فقط**: `"Segoe UI", "Dubai", "Noto Sans Arabic", Tahoma, sans-serif`.
- **ممنوع الاعتماد على اللون وحده** — كل حالة لها أيقونة ونص (لضعاف تمييز الألوان).
- **الموبايل أولًا** من 360px — النظام سيُفتح من الموبايل أكثر من الديسكتوب.
- وضع داكن وفاتح عبر `prefers-color-scheme` مع تبديل يدوي.
- المخططات SVG مولّدة من السيرفر — بلا مكتبة.
- احترام `prefers-reduced-motion`.

التفاصيل الكاملة (التوكنات بقيم فعلية، كل شاشة) في **`docs/UI-SPEC.md`**.

---

## 11. الأمن

| التهديد | السيناريو | الضابط |
|---|---|---|
| **SSRF** | الأدمن يدخل `http://169.254.169.254` ← سيرفرنا يجلب بيانات اعتماد السحابة ويعرضها | `checks/guard.js`: حظر على **الـ IP المحلول** لا النص · إعادة الفحص **بعد كل تحويلة** · منافذ 80/443 فقط · بروتوكولات http/https فقط |
| **XSS** | موقع عميل مخترق يضع `<script>` في `<title>` ونحن نعرضه في لوحتنا | تهريب HTML على كل مُخرَج + CSP صارمة |
| **IDOR** | عميل يغيّر `/site/12` إلى `/site/13` | `assertOwnership` على كل مسار فيه معرّف |
| **رفع الصلاحيات** | عميل يرسل `role=admin` في نموذج | قائمة حقول مسموحة صريحة — لا `Object.assign` من جسم الطلب |
| **حجم الرد** | موقع يرجع 10 جيجابايت | سقف 512KB في `fetch.js` (مُطبَّق) |
| **حلقة تحويلات** | تحويل لا نهائي | حد 5 تحويلات (مُطبَّق) |

التفاصيل الكاملة في **`docs/SECURITY.md`**.

---

## 12. الفوترة والصيانة والتذاكر

- **الفلوس بالقروش** (أعداد صحيحة) — `REAL` يسبب أخطاء تقريب.
- **فواتير دورية**: عقد الصيانة الشهري هو نموذج العمل الأغلب — توليد تلقائي.
- **المتأخرات** تُعرض بنبرة مهنية لا تحصيلية — هذه لوحة عميل لا إنذار.
- **الملخص الشهري**: صفحة «ما تم عمله لموقعك هذا الشهر» تُولَّد آليًا من سجل الصيانة
  والأعطال. أقوى ميزة احتفاظ، وتكلفتها البرمجية شبه صفر لأن البيانات موجودة.
- **الإشعارات**: داخل البوابة فقط في v1 (شارة + قائمة). لا خدمة بريد خارجية.
- **طباعة الفاتورة**: `@media print` — بلا مكتبة PDF.

---

## 13. التشغيل

- `systemd` unit بمستخدم غير جذري وإعادة تشغيل تلقائية.
- **النسخ الاحتياطي**: `VACUUM INTO` (آمن أثناء التشغيل مع WAL) — لا نسخ ملفات.
- السجلات بتنسيق منظّم مع تدوير داخلي. **لا تُسجَّل أبدًا**: كلمات سر أو توكنات.
- نقطة `/health` للمراقبة الذاتية.

---

## 14. استراتيجية الاختبار

بيئة التطوير **تحجب كل الدومينات الخارجية** وتستبدل شهادات SSL ببروكسي.
لذلك الاختبار محلي بالكامل بـ `node:test` المدمج:

| الملف | يغطي |
|---|---|
| `test/fingerprint.test.js` | سيرفر محلي يقلّد ووردبريس/شوبيفاي/سلة/زد/ويكس بـ HTML وهيدرات وكوكيز حقيقية |
| `test/guard.test.js` | SSRF: localhost · 127.x · 10.x · 192.168.x · 169.254.169.254 · تحويل لداخلي |
| `test/fetch.test.js` | 500 · تعليق · حلقة تحويلات · رد ضخم |
| `test/health.test.js` | معادلة الدرجة والحالات الحدّية |
| `test/isolation.test.js` | عميل لا يستطيع قراءة بيانات عميل آخر |
| `test/auth.test.js` | scrypt · CSRF · تحديد المحاولات · تعداد الحسابات |

---

## 15. مراحل التنفيذ

### ✅ المرحلة 1 — الأساس (تمّت)
`package.json` بصفر تبعيات · `schema.sql` · `db.js` · `auth.js` ·
`fetch.js` و `tls.js` و `dns.js` (مُختبَرة حيًا) · `fingerprint.js` (26 منصة)

### 🔄 المرحلة 2 — إغلاق محرك الفحص
**معيار "تم"**: `node --test` يمر بالكامل.
- [ ] `checks/guard.js` + ربطه داخل `fetch.js`
- [ ] `checks/headers.js`
- [ ] `checks/run.js` + معادلة الدرجة
- [ ] `test/` كاملة

### 🔄 المرحلة 3 — المراقبة
**معيار "تم"**: تشغيل النظام ساعة يسجّل فحوصات وأعطالًا صحيحة.
- [ ] `monitor.js` · الجدولة · الفحص المرجعي · آلة حالات العطل · سجل الفجوات
- [ ] وظيفة التجميع اليومي

### 🔄 المرحلة 4 — الويب
**معيار "تم"**: عميل تجريبي يدخل ويرى موقعه وفواتيره.
- [ ] راوتر + جلسات + CSRF · تفعيل · دخول · إعادة تعيين
- [ ] لوحة العميل + بطاقة MVP · تفاصيل الموقع · الفواتير · التذاكر
- [ ] لوحة الأدمن (عملاء · مواقع · صيانة · فواتير · طلبات إعادة التعيين)

### 🔄 المرحلة 5 — التسليم
- [ ] `seed.js` · دليل التشغيل · فحص عدائي نهائي

---

## 16. ما لن نبنيه في v1 (ولماذا)

| البند | السبب |
|---|---|
| بلَجن ووردبريس | يخالف القيد الثاني |
| تكامل شوبيفاي | يخالف القيد الثاني |
| إشعارات بريد/SMS | تتطلب خدمة خارجية |
| دفع أونلاين | يتطلب بوابة دفع خارجية |
| تقارير PDF | مكتبة خارجية — البديل `@media print` |
| مراقبة من مواقع متعددة | يتطلب سيرفرات إضافية — يُذكر كقيد معلن |

---

## 17. القيود المعروفة (بصراحة كاملة)

1. **بيئة التطوير تحجب الإنترنت** وتستبدل شهادات SSL. الكود سليم، لكن **التحقق النهائي
   لا يصح إلا على سيرفرك**.
2. **دقة كشف المنصة خارجيًا ~85–90%**. النظام يعرض **نسبة الثقة والدليل**،
   ويمكن للأدمن كتابة المنصة يدويًا.
3. **المراقبة من مكان واحد**: لو سيرفرك وقع، تتوقف المراقبة — لذلك بُني سجل الفجوات.
4. **SQLite تكفي حتى ~5000 موقع** على سيرفر واحد. طبقة `db.js` معزولة لتسهيل النقل لاحقًا.
5. **`node:sqlite` متزامنة** وتحجب حلقة Node. عند آلاف المواقع يلزم نقل الفحص
   إلى `worker_threads`. غير مطلوب في v1.

---

## 18. قرارات تحتاج كلمتك

1. **العملة والضريبة**: جنيه مصري فقط أم متعدد؟ وهل تُضاف ضريبة على الفواتير؟
2. **فترة الفحص الافتراضية**: كل 5 دقائق مناسبة؟ (أقل = دقة أعلى وحمل أكبر)
3. **التذاكر في v1**: أبنيها أم أؤجلها وأكتفي بواتساب؟
4. **الأرقام في الواجهة**: عربية (1234) أم هندية (١٢٣٤)؟
5. **لغة الواجهة**: عربي فقط أم عربي/إنجليزي؟
