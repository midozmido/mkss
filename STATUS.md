# نقطة الحفظ — أين وصلنا

> آخر تحديث: 2026-09-13 · الفرع: `claude/customer-support-sites-system-qm032c`
> الحالة: **متوقف بطلب المستخدم**. استكمال العمل ينتظر كلمة "كمّل".

---

## 1. ما تم إنجازه فعليًا (مرفوع على الفرع)

### الأساس — صفر اعتماديات
| الملف | الحالة | ملاحظة |
|---|---|---|
| `package.json` | ✅ | `dependencies` فاضية — `npm install` لا يُنفّذ |
| `.gitignore` | ✅ | يستثني `data/*.db` |
| `src/schema.sql` | ✅ | 12 جدول — مُطبَّق ومُختبَر |
| `src/db.js` | ✅ | `node:sqlite` + WAL + مفاتيح أجنبية |
| `src/auth.js` | ✅ | scrypt · جلسات · CSRF · تحديد المحاولات |

### محرك الفحص
| الملف | الحالة | التحقق |
|---|---|---|
| `src/checks/fetch.js` | ✅ | **مُختبَر حيًا** — TTFB بدقة نانوثانية، تتبع تحويلات، أخطاء بالعربي |
| `src/checks/tls.js` | ✅ | **مُختبَر حيًا** — قرأ شهادة وحسب أيامها وتحقق من تغطية الدومين |
| `src/checks/dns.js` | ✅ | **مُختبَر حيًا** — استنتج مزوّد الـ DNS من الـ nameservers |
| `src/checks/fingerprint.js` | ✅ مكتوب · ⚠️ غير مُختبَر | 26 منصة · يشمل سلة وزد · لم يُختبر لأن البيئة تحجب المواقع |

### الوثائق
- `PLAN.md` — الخطة **الأولى** (قبل تعديلات المستخدم). **تحتاج إعادة كتابة.**

---

## 2. قيود جديدة من المستخدم (تغيّر المعمار)

### قيد مقدّس 1 — صفر اعتماديات خارجية
> "مش عايز اى حاجه مبنيه على حاجه خارجية ابدا"

ممنوع: أي حزمة npm · أي خدمة SaaS · أي CDN أو خط خارجي.
مسموح فقط: وحدات Node 22 المدمجة + HTML/CSS/JS خام.

### قيد مقدّس 2 — العميل يدخل بإيميل وباسورد بس
> "اهم حاجه عندى ان العميل يخش بايميل وباسورد بس ميدخلش ف حوارات تانية خالص"

**الأثر المعماري:** العميل عمره ما يركّب بلَجن، ولا ينسخ توكن، ولا يضيف موقع،
ولا يعمل أي إعداد. كل الإعداد من لوحة الأدمن.
← **v1 يعتمد حصريًا على الفحص الخارجي (Tier 0).** طبقات Tier 1 و 2 اتشالت من v1 بالكامل.

### طلبات إضافية
- أحدث أساليب الـ UI في تكوين "البرنامج 2 في 1" (بوابة الدعم + لوحة الـ MVP).
- "خطة متخرش المايه" = إحكام أقصى.
- **عدد وكلاء أقل** حفاظًا على التوكن.

---

## 3. الاقتراحات اللي اتبعتت للمستخدم (في انتظار رده)

1. **استعادة كلمة السر بدون خدمة إيميل** — زرار "طلب إعادة تعيين" يفتح طلبًا في لوحة
   الأدمن، والأدمن يولّد رابطًا لمرة واحدة ويبعته واتساب. يحقق: صفر خدمة خارجية +
   صفر مجهود على العميل + مناعة ضد إقفال الحسابات بالتعداد.
2. **الواجهة تحكي ولا تعرض أرقامًا** — «شغّال من 47 يوم بدون انقطاع» بدل «Uptime 99.97%».
   لأن العميل لا يعدّ شيئًا، القيمة كلها في ما يراه.
3. **الملخص الشهري** — صفحة «اللي عملناه لموقعك الشهر ده» تتولد آليًا من سجل الصيانة
   والأعطال. أقوى ميزة احتفاظ، وتكلفتها البرمجية شبه صفر.
4. **سد ثغرة SSRF من التصميم** — الأدمن يدخل رابطًا وسيرفرنا يجلبه؛ يجب منع
   `localhost` و `169.254.169.254` والشبكات الخاصة، بفحص على الـ IP المحلول
   وإعادة الفحص بعد كل تحويلة.
5. **سجل فجوات المراقبة** — لو سيرفر المراقبة وقع ساعتين، ممنوع نحسبها "شغّال".
   تُسجَّل كفجوة. نظام يكذب مرة يخسر الثقة للأبد.

---

## 4. العمل المتوقف (جاهز للاستئناف)

**وركفلو تحصين الخطة** — 5 وكلاء: 3 عدسات تصميم (الأساس/المحرك/الواجهة) +
ناقد عدائي واحد + كاتب وثائق.

- سكربت جاهز: `~/.claude/projects/.../workflows/scripts/mkss-plan-lean.js`
- اتوقف في مرحلة التصميم، **لم يكتب أي ملف**.
- المخرجات المتوقعة عند الاستئناف: إعادة كتابة `PLAN.md` + `docs/UI-SPEC.md`
  + `docs/SECURITY.md` + `docs/TEST-PLAN.md`.

---

## 5. الخطوة التالية عند الاستئناف

1. تشغيل وركفلو تحصين الخطة → الوثائق الأربع.
2. عرض الخطة المنقّحة والاقتراحات على المستخدم لأخذ قراره.
3. بعد الموافقة، استكمال البناء بالترتيب:
   - `src/checks/headers.js` (هيدرات الأمان) + حماية SSRF داخل `fetch.js`
   - `src/checks/run.js` (منسّق الفحص + درجة الصحة)
   - `test/` (اختبارات محلية — ضرورية لأن البيئة تحجب المواقع الخارجية)
   - `src/monitor.js` (الجدولة + الأعطال + سجل الفجوات)
   - طبقة الويب (راوتر · دخول · لوحة العميل · تفاصيل الموقع · فواتير · تذاكر · أدمن)
   - `seed.js` + دليل التشغيل + فحص عدائي نهائي

---

## 6. قيود البيئة (للتذكير عند الاستئناف)

- حاوية التطوير **تحجب كل الدومينات الخارجية** (`Host not in allowlist`).
- **تستبدل شهادات SSL** ببروكسي، فقراءة الشهادات هنا لا تعبّر عن الواقع.
- ← الاختبار لازم يكون محليًا بالكامل: `node:test` + سيرفر HTTP محلي بملفات HTML مصغّرة.
- ← التحقق النهائي لا يصح إلا على سيرفر المستخدم.
