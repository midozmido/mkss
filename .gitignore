node_modules/
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# إيصالات العملاء. ملفٌّ رفعه عميل لا يدخل مستودعًا أبدًا — لا في فرع
# خاص ولا في لقطة، لأن ما يدخل تاريخ Git لا يخرج منه.
data/uploads/
data/backups/
.env
.DS_Store

# حارس المجلد يُتعقَّب رغم تجاهل محتوياته — بلا رفعه يصير المجلد مكشوفًا
!data/.htaccess
