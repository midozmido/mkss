# ابدأ من هنا — ناشر المقالات الآلي على ووردبريس

## إيه ده؟

أداة بتخلّي مقالاتك تنزل على موقع ووردبريس **أوتوماتيك**. بتكتب المقال ملف نصي عادي،
والأداة بتحوّله لمقال ووردبريس كامل: عنوان، تنسيق، تصنيفات، وسوم، صورة بارزة، ملخص، وسيو.

بتشتغل عبر REST API الرسمي لووردبريس و Application Password — الطريقة المدعومة رسميًا،
**مش محتاجة تركّب أي بلجن على موقعك**.

ومعاها **مهارة Claude** جاهزة، فلو بتستخدم Claude Code مش محتاج تحفظ أي أوامر — قول له
"انشر المقالات على موقعي" وهو يمشي العملية كلها بنفسه.

## لمين الأداة دي؟

أي حد عنده موقع ووردبريس وبيكتب عليه بانتظام. مفيدة أكتر لو:

- بتنشر أكتر من مقال في الأسبوع
- عندك تقويم محتوى وعايز المقالات تتجدول لوحدها
- بتكتب المقالات بمساعدة AI وتعبت من النسخ واللصق في محرر ووردبريس
- عايز المقالات محفوظة عندك في ملفات ومتتبّعة في git، مش محبوسة جوه الموقع

## التشغيل في 3 دقايق

### 1. هات Application Password من موقعك

من لوحة تحكم ووردبريس:

```
Users → Profile → انزل لتحت لـ Application Passwords → Add New
```

اكتب أي اسم (مثلاً `Publisher`) واضغط Add. هيطلعلك كود شكله كده:

```
Ab3d Ef7h Ij2k Lm9n Op4q Rs8t
```

**انسخه فورًا** — مش هيظهر تاني. وده **مش** باسورد دخولك العادي، ده باسورد منفصل
للتطبيقات وتقدر تلغيه في أي وقت بدون ما تأثر على حسابك.

### 2. ظبّط الأداة

```bash
cd wp-publisher
pip install -r requirements.txt
cp .env.example .env
```

وافتح `.env` واملأ 3 حاجات:

```
WP_SITE_URL=https://your-site.com
WP_USERNAME=اليوزرنيم بتاعك في ووردبريس
WP_APP_PASSWORD=Ab3d Ef7h Ij2k Lm9n Op4q Rs8t
```

### 3. اختبر الاتصال

```bash
python3 wp_publish.py doctor
```

الأمر ده بيختبر كل حاجة ويقولك بالظبط إيه الشغال وإيه المشكلة لو فيه:
الوصول للموقع، المصادقة، وكل صلاحية لوحدها (نشر / تعديل / رفع صور / تصنيفات).

**لو عدّى، إنت جاهز.** لو لأ، الرسالة نفسها بتقولك السبب والحل، وفيه جدول أعطال كامل
في [`wp-publisher/README.md`](wp-publisher/README.md).

## بعد كده — الطريقة السهلة

لو بتستخدم **Claude Code**، مش محتاج تحفظ أوامر. المهارة مركّبة في الريبو، فكفاية تقول:

> انشر المقالات على موقعي

> اكتب مقال عن كذا وانشره كـ draft

> جدول المقال ده ينزل يوم الأحد الصبح

وكلود هيعمل الباقي: يكتب الملف بالشكل الصح، يعمل معاينة، ويسألك قبل أي نشر مباشر.

## الطريقة اليدوية

اكتب ملف `.md` في `wp-publisher/content/` كده:

```markdown
---
title: عنوان المقال
categories: [أخبار]
tags: [وسم أول, وسم تاني]
---

نص المقال بالـ Markdown العادي: **عريض**، *مائل*، قوائم، جداول، صور، وكود.
```

وبعدين:

```bash
python3 wp_publish.py publish --all --dry-run   # معاينة — مش بتكتب أي حاجة على الموقع
python3 wp_publish.py publish --all             # ينزّل كـ draft للمراجعة
python3 wp_publish.py publish --all --status publish   # نشر مباشر
```

ابدأ من [`wp-publisher/content/_template.md`](wp-publisher/content/_template.md) — فيه كل
الحقول المتاحة مشروحة.

## 4 حاجات مهمة تعرفها

**١. النشر المباشر لازم تطلبه.** المقال الجديد بينزل `draft` افتراضيًا. بينزل منشور
في حالتين بس: لو الملف نفسه فيه `status: publish`، أو لو شغّلت `--status publish`.
والمقال المنشور **مش بيرجع draft** لو شغّلت الأداة تاني — إلا لو طلبت كده صريح.

**٢. التشغيل مرتين مش بيعمل مقالات مكررة.** الأداة بتربط كل ملف برقم مقاله، فلو موجود
بتحدّثه ولو مش موجود بتعمله. فآمن تشغّلها كل يوم أو تحطها في cron. ولو ملفين اتصادموا
على نفس المقال، بتوقف وتقولك بدل ما تمسح حاجة.

**٣. الباسورد في `.env` بس.** الملف ده متجاهَل في git ومش هيترفع مع الكود. متحطّهوش
في أي ملف تاني. ولو اتكشف في أي وقت، امسحه من نفس صفحة Application Passwords
واعمل واحد جديد — الإلغاء فوري.

**٤. اعمل يوزر مخصص للنشر.** الـ Application Password بيشتغل بكل صلاحيات اليوزر بتاعه.
لو عملته من حساب Administrator، الكود ده يقدر يعمل حاجات أكتر بكتير من نشر مقال.
الأفضل تعمل يوزر جديد بدور **Author** أو **Editor** وتعمل الباسورد منه.

**٥. الموقع لازم يكون https.** الأداة بترفض `http` لأن الباسورد بيتبعت مكشوف على الشبكة
(ينفع تتخطاها بـ `WP_ALLOW_HTTP=1` لو ده سيرفر محلي بس).

## إيه اللي جوه الباكدج

```
START-HERE.md                        ← الملف ده
wp-publisher/
├── README.md                        الدليل الكامل + جدول استكشاف الأعطال
├── wp_publish.py                    الأداة: doctor / list / publish
├── markdown_blocks.py               تحويل Markdown لبلوكات ووردبريس
├── content/_template.md             قالب المقال بكل الحقول
├── tests/                           191 اختبار + سيرفر ووردبريس مزيّف
├── .env.example                     قالب البيانات
└── requirements.txt
.claude/skills/wp-publisher/         مهارة Claude — بتشغّل الأداة لوحدها
.github/workflows/publish-articles.yml   نشر تلقائي بجدول عبر GitHub Actions
```

## المتطلبات

- **Python 3.9 أو أحدث** (`python3 --version`)
- مكتبتين: `requests` و `PyYAML` — بيتركبوا بأمر واحد من `requirements.txt`
- موقع ووردبريس 5.6 أو أحدث (Application Passwords موجودة افتراضيًا من الإصدار ده)

## الأداة مختبرة، مش كلام

معاها سيرفر ووردبريس **مزيّف** كامل، فتقدر تتأكد إن كل حاجة سليمة من غير ما تلمس
موقعك الحقيقي:

```bash
cd wp-publisher && python3 -m unittest discover -s tests -v
```

**191 اختبار** بتغطي: النشر والتحديث ومنع التكرار، رفع الصور ومنع تكرارها،
التصنيفات والجدولة والسيو، المحتوى العربي، تحويل الـ Markdown والحماية من حقن الكود،
حصر مسارات الصور ومنع SSRF، الجدولة بتوقيت الموقع، وكل حالات الفشل (باسورد غلط،
ترويسة محجوبة، صلاحية ناقصة، REST مقفول، السيرفر بيقع ويرجع، ملفات بايظة).

والسويت نفسها اتفحصت بـ **اختبار الطفرات**: عطّلنا كل حماية في الكود واحدة واحدة
وشوفنا الاختبارات بتكشفها ولا لأ — واللي مكانش بيتكشف اتكتبله اختبار.

وللتجربة اليدوية بدون موقع حقيقي:

```bash
python3 tests/mock_wp.py      # بيقوم سيرفر مزيّف ويطبع بياناته جاهزة للنسخ
```

## الأتمتة الكاملة

بعد ما تتأكد إن النشر شغال، تقدر تخليها تمشي لوحدها:

| الطريقة | الشرح |
|---|---|
| **GitHub Actions** | جاهز في الريبو — بينشر عند أي تعديل على `content/` أو بجدول يومي. محتاج بس تحط البيانات في Settings → Secrets → Actions |
| **cron** | `0 9 * * * cd /path/wp-publisher && python3 wp_publish.py publish --all` |
| **n8n** | بدون كود: `Schedule` → `HTTP Request` بـ Basic Auth |

التفاصيل في [`wp-publisher/README.md`](wp-publisher/README.md).
